---
layout: base
pagination:
  data: collections.regions
  size: 1
  alias: region
permalink: "region/{{ region.name | slugify }}/index.html"
eleventyComputed:
  title: "{{ region.name | default: 'Регион' }} — Модели и фотографы | ModelConnect Miami"
  description: "Профили моделей, фотографов и стилистов из региона {{ region.name | default: 'не указан' }}. ModelConnect Miami — платформа для творческих людей."
---
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <meta name="description" content="{{ description }}">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <nav>
      <a href="/">Главная</a> ›
      <a href="/region/">Регионы</a> ›
      <span>{{ region.name | default: 'Регион' }}</span>
    </nav>
  </header>

  <main>
    <h1>Регион: {{ region.name | default: 'Не указан' }}</h1>

    {% if region and region.profiles and region.profiles.length > 0 %}
      <ul class="profiles-list">
        {% for profile in region.profiles %}
          <li class="profile-card" style="display: flex;">
            <img
              src="{{ profile.image_url | default: '/img/no-avatar.png' }}"
              alt="{{ profile.name | default: 'Без имени' }}"
              class="profile-card__image"
              width="300"
              height="200"
            >
            <div class="profile-card__description">
              <h2>{{ profile.name | default: 'Без имени' }}</h2>
              <p>{{ profile.city | default: 'Регион не указан' }}</p>
              <p>{{ profile.description | truncate: 100 }}</p>
              <a href="/profiles/{{ profile.id | slugify }}/index.html">Подробнее</a>
            </div>
          </li>
        {% endfor %}
      </ul>
    {% else %}
      <p>В этом регионе пока нет профилей.</p>
    {% endif %}
  </main>
</body>
</html>