---
layout: base
title: "ModelConnect Miami — Поиск по регионам"
description: "Смотрите профили моделей, фотографов и стилистов по регионам Майами."
permalink: /region/index.html
---

<nav class="breadcrumbs">
  <a href="/">Главная</a> /
  <span>Регионы</span>
</nav>

<div class="regions-section" id="regions">
  <h1>Профили по регионам</h1>

  {% assign regions = collections.profiles | map: "data.profile.city" | uniq %}

  {% for region in regions %}
    <h2 class="region-title" data-region="{{ region | downcase }}">{{ region }}</h2>
    <div class="profile-list" data-region="{{ region | downcase }}">
      {% for p in collections.profiles %}
        {% if p.data.profile.city == region %}
        <div class="profile-card">
          <img
            class="profile-card__image"
            width="300"
            height="200"
            src="{{ p.data.profile.image_url | default: '/img/placeholder.png' }}"
            alt="Фото профиля {{ p.data.profile.name }}"
          />
          <div class="profile-card__description">
            <h3>{{ p.data.profile.name }}</h3>
            <p>{{ p.data.profile.city }}, {{ p.data.profile.category }}</p>
            <a href="{{ p.url }}">Подробнее</a>
          </div>
        </div>
        {% endif %}
      {% endfor %}
    </div>
  {% endfor %}
</div>

<script>
document.addEventListener("DOMContentLoaded", function () {
  const hash = window.location.hash.replace("#", "").toLowerCase();
  if (hash) {

    document.querySelectorAll(".region-title, .profile-list").forEach(el => {
      el.style.display = "none";
    });

    document.querySelectorAll(`[data-region='${hash}']`).forEach(el => {
      el.style.display = "block";
    });
  }
});
</script>
