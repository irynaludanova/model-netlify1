---
layout: base
title: Профили
---
<h1>Профили</h1>

<ul class="profiles-list">
  {% for item in collections.profiles %}
    {% assign p = item.data.profile %}
    <li class="profile-card">
      <a href="{{ p.url }}">
        <img src="{{ p.image_url | default: '/img/placeholder.png' }}" alt="{{ p.name }}" width="120" height="120" />
        <strong>{{ p.name }}</strong>
        <div>{{ p.city }} — {{ p.category }}</div>
      </a>
    </li>
  {% endfor %}
</ul>
