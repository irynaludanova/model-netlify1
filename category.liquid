---
layout: base
permalink: /category/index.html
eleventyComputed:
  title: '{{ site.name }} — Категории'
  description: 'Обзор всех категорий моделей, фотографов и стилистов Майами 2025'
  seo_og_image: '{{ site.url }}{{ site.seo_og_image }}'
  seo_og_url: '{{ site.url }}/category'
  seo_canonical: '{{ site.url }}/category'
  seo_json_ld: |
    {
      "@context": "https://schema.org",
      "@type": "WebPage",
      "name": "{{ site.name }} — Категории",
      "url": "{{ site.url }}/category",
      "description": "Обзор всех категорий моделей, фотографов и стилистов Майами 2025"
    }
---

<section class="profiles-section">
  <h1>Категории</h1>

  <div id="filters-container">
    <label>Категория:
      <select id="category-select"><option value="все">Все</option></select>
    </label>
    <label>Регион:
      <select id="region-select"><option value="все">Все</option></select>
    </label>
  </div>

  <div id="profile-list">
    <p>Загрузка профилей...</p>
  </div>

  <div id="pagination-container"></div>
</section>
